// configuration
var config = {
    env: "production",
    host: "localhost",
    port: 3000,
    charset: "utf8",
    restURL: "https://petshelterapi-jamiu.herokuapp.com/"
};

module.exports = config;